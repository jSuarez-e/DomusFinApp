# Use Cases Layer

Contains pure business logic use cases (application service orchestrators) which interact with domain repositories and entities.
This layer is decoupled from frameworks, HTTP controllers, and ORMs.
